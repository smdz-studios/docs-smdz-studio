// SMDZ Studios — Global announcement bar configuration
// Only you should edit THIS file (do not expose it to public editors).
// This configuration is read by index.html to render the banner.

window.SMDZ_BANNER_CONFIG = {
  // Enable or disable the banner globally
  enabled: true,

  // Type controls the color style via CSS:
  // "info" | "alert" | "sale" | "maintenance"
  type: "info",

  // HTML is allowed here (for links, bold text, icons, etc.)
  text: `
    📢 <strong>Notice:</strong> SMDZ Studios documentation is in active development.
    Some pages may change frequently as new scripts and updates are released.
  `
};